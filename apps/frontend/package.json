{
  "name": "@alo-retail-pos-service/frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build --configLoader native",
    "dev": "vite --host 0.0.0.0 --configLoader native",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@alo-retail-pos-service/pos-domain": "*",
    "preact": "^10.27.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "@shopify/polaris-types": "latest",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
