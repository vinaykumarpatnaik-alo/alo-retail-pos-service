name: Deploy Dev

on:
  workflow_dispatch:
  push:
    branches:
      - develop*
    paths-ignore:
      - "extensions/**"

concurrency:
  group: deploy-dev
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/reusable-deploy.yml
    secrets: inherit
    with:
      env_name: dev
      account_id: "891377288714"
